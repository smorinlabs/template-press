# Session handoff — Template Press P12 closeout and P03/P07 delivery — 2026-09-10

## Outcome

P12 is complete: four corrections merged in PR #125, one follow-up was already
complete, and the owner declined implementation of the other six. The closeout
and handoff merged in PR #126. PR #127 merged the P03/P07 tracking
reconciliation. The owner then approved the three recommended changes:
explain reuse of an existing `press/` directory, reuse the plan's validated
inventory for that advice, and strengthen the platform-parser annotation.
The implementation and review dispositions are delivered through
[PR #131](https://github.com/smorinlabs/template-press/pull/131), currently
open. Local validation passed and both historical P07 threads are resolved.
The next session must confirm PR review, CI and merge state before selecting
further work. P03-M6 provision/status implementation remains open.

Scope is Template Press only. Gmail2PDF, downstream feedback-log reassessment,
new P11A optimization, Windows job splitting, persistent diagnostics, releases,
dependency updates and unrelated checkout cleanup are outside this task.
This document carries the decisions and next actions inline; linked records
provide the supporting detail.

## Portability and repository identity

- Repository: `https://github.com/smorinlabs/template-press.git`; default branch:
  `main`. Use a current checkout of this repository on another machine.
- P12 implementation merged in [PR #125](https://github.com/smorinlabs/template-press/pull/125)
  at `9b265813a4ce36da6ae3e170e914949b480a4eab`.
- The P12 documentation branch was `docs/p12-closeout`. Commit
  `069c5d16cdbc79d0cf99548d568ea383c768e7ee` records the six owner declines and
  closes P12. [PR #126](https://github.com/smorinlabs/template-press/pull/126)
  merged the closeout and initial refreshed handoff on 2026-09-10 at
  `b07d60f290af821806597e9f12523119aadd8493`.
- [PR #127](https://github.com/smorinlabs/template-press/pull/127) merged
  the P03/P07 reconciliation at `6ebba668cc260121ae3c620733df9abb2bcb79fe`.
- The implementation delivery branch is `feature/p03-p07-followups`, based
  on `8cadf9445be6813e6c8ea6de4baa2a3c518fd99b`. That base also includes the
  independently merged release PR #110. Implementation commit
  `ee30a763d26ec81a95dccbe6b42ee516dcc6966c` is pushed in PR #131. The project
  completion markers take effect when that PR merges. Fetch its latest head
  or containing merge before resuming; local files alone do not travel.
- Same-machine delivery checkout: `/private/tmp/template-press-p03-p07-followups`.
  All paths below are relative to that repository root. This temporary path
  is a convenience, not a portable source.
- The older checkout `/Users/stevemorin/c/template-press-pr90-edit` remains on
  `feat/edit-table` at `f787955fe425b3c7b162a60b81044d07a17f2036`. That commit
  saved the original handoff, but its product source predates merged P12.
  Do not use that branch as the implementation base.
- The older checkout also contains the unrelated untracked file
  `docs/handoffs/2026-09-04-pr116-closeout-and-next-milestones.md`. It will not
  travel through this delivery. Preserve it.
- The earlier inventory found local `main` at
  `/Users/stevemorin/c/template-press`, behind the remote with unrelated
  untracked handoffs. Preserve that checkout and other worktrees; refresh
  their state before considering any separately authorized maintenance.

## Artifacts

The following records are tracked and substantive. Their absolute same-machine
paths are the delivery checkout root above plus the listed repository-relative
path. The P12 closeout and initial P03/P07 reconciliation are on remote `main`.
The implementation, new regression tests, updated CLI reference, project
dispositions and this revision travel together on the implementation branch.
Use PR #131's latest pushed head to obtain the final receipts. The temporary
checkout and diagnostic logs are not required to continue on another machine;
the decisions and material validation results are recorded here.

| Record | Repository-relative path | Role |
| --- | --- | --- |
| Project trunk | `PROJECTS.md` | P12 complete; P03-M4d and P07-T07 delivery status; P03-M6 remains open |
| P12 project | `projects/P12-origin-guard-and-diagnostics.md` | All eleven dispositions, accepted limitations and reopening conditions |
| P12 evaluation | `docs/superpowers/reviews/2026-09-10-p12-value-evaluation.md` | Historical probes and recommendations, with the subsequent owner decision |
| Design contract | `docs/superpowers/specs/2026-09-01-press-improvements-g2p-design.md` | Accepted behavior, including the explicitly accepted E5 warning gap |
| P03 project | `projects/P03-external-target-rebrand-press-.md` | M4d decisions, implementation and measured benefit; unimplemented M6 |
| P07 project | `projects/P07-platform-conditional-declared-commands.md` | Historical native validation and T07 comment dispositions |
| CLI reference | `docs/source/reference/cli.md` | Directory reuse warning and ordinary-content handling |
| Regression coverage | `tests/rebrand/test_control_directory_advice.py` | Preview/apply output, ordinary content, snapshot reuse and fresh later plans |
| This handoff | `docs/handoffs/2026-09-10-p12-post-merge.md` | Portable closeout and next-session instructions |

## P12 completed work and owner decisions

Task markers distinguish implementation completed (`[x]`) from implementation
declined (`[-]`). Closing a declined task does not claim its limitation was
fixed. The owner's instruction was: "Yes close the 6 recommended to close and
document the pass on those".

The five completed follow-ups are:

- `P12-T-defer-2`: removal warnings count excluded tracked descendants carried
  by directory renames, deduplicated in original source coordinates.
- `P12-T-defer-4`: five retained links into deliberately removed research use
  named historical references.
- `P12-T-defer-5`: Windows recovery hints use labeled JSON argument arrays;
  POSIX quoting and conditional restoration behavior remain intact.
- `P12-T-defer-6`: verification accepts an origin mismatch only when the exact
  value matches a receipt bound to the target identity. This was already
  complete before the four corrections in PR #125.
- `P12-T-defer-7`: the existing `rmdir_paths` JSON field is documented.

The six declined follow-ups are closed without implementation:

| Task | Decision and accepted limitation | Reopen only when |
| --- | --- | --- |
| `P12-T-defer-1` | Pass on batching ignore-hint queries. The historical synthetic case took 73.211 seconds for 1,000 Git processes across 500 findings, but no affected real workload was found. | A representative target shows material advice-generation cost and batching preserves literal paths, ignore inputs and fallback behavior. |
| `P12-T-defer-3` | Accept the narrow missing removal warning for a correctly retargeted stable-name symlink. This is an explicit gap in E5's broad wording; this repository has no tracked symlinks. | A real target needs the warning and a bounded design preserves containment and retargeting safety. |
| `P12-T-defer-8` | Retain prefix-warning counts from original source content. Moving counts after declared replacements could add warnings as well as remove them. This supersedes the earlier PR #109 triage promise. | An affected target justifies an explicitly approved warning policy. |
| `P12-T-defer-9` | Retain stderr, exit 2 and empty stdout for operational `press verify --json` preflight refusals. The contract is intentional and tested. | A real consumer needs a designed, compatible machine-readable refusal format. |
| `P12-T-defer-10` | Retain the stable-input prerequisite for `press clean` and all existing guards. An arbitrary writer can still replace a path after the final check; another check merely moves the race. | A required concurrent workflow justifies explicit coordination or platform-specific guarantees. |
| `P12-T-defer-11` | Pass on caching directory-removal identity checks. The historical native guard cost was 0.004808 seconds, about 0.16% of a 2.975-second freeze. | Representative timings show a useful benefit and the design preserves identity, missing-input and concurrent-change guards. |

There are no open P12 follow-up decisions or implementation tasks. Reopening
conditions are reassessment criteria, not authorization for new work.

## Validation and delivery evidence

- PR #125's final head was `bafdfbfcaefc8b6c716e0f0561d638b6dd7752a0`.
  Its [shipping CI](https://github.com/smorinlabs/template-press/actions/runs/34444700728)
  passed before merge. The [native Windows suite](https://github.com/smorinlabs/template-press/actions/runs/34444700728/job/102766912956)
  reported 2,123 passed and 110 skipped. Native Windows R3 acceptance also
  passed in [run 34444700693](https://github.com/smorinlabs/template-press/actions/runs/34444700693).
- The documentation closeout ran `just setup` and the complete `just check`
  pipeline locally. With the locked pytest-xdist plugin and
  `PYTEST_ADDOPTS='-n 8'`, pytest reported 2,193 passed and 24 skipped in
  760.53 seconds. Lint, type checking, YAML, spelling, EditorConfig and commit
  hooks passed. Existing YAML warnings were nonblocking.
- Documentation consistency checks verified exactly six declined and five
  completed P12 follow-ups, matching project/trunk completion, decision rows,
  local references and new anchors. The closeout changes Markdown only.
- PR #126 passed all eight required GitHub checks and its applicable validation
  jobs. The test, typecheck and build jobs were intentionally skipped by the
  workflow's Markdown-only condition. CodeRabbit and Copilot completed review;
  the style suggestions received reasoned declines, and the final authoritative
  thread check showed no unresolved threads. Merge was bound to reviewed head
  `cb4c8e10a4f042b4e18bfa1178263c33855e3f33`.
- These facts distinguish implementation delivery, documentation validation,
  package publication and downstream retesting. No new release or downstream
  retest is claimed. Historical probe timings above were not remeasured for
  this closeout. No new external review is claimed.

## P03/P07 implementation and remaining work

The owner approved the following work after the value evaluation on
2026-09-10. P03 remains in progress for M6 after this delivery merges.
Its prerequisite, issue #42, is satisfied through P06. Direct command probes
confirmed `press provision` and `press status` still exit 2 with "coming in M6".
Closing the dependency did not implement those commands.

| Item | Implemented or accepted decision | Value and constraint |
| --- | --- | --- |
| P03-M4d (1), discovery preview | An unmarked root `press/` receives a specific warning that it "would" hold configuration and receipts. | Makes the planned reuse explicit while preserving byte-for-byte read-only preview. Source-config writes still follow every exit-2 gate. |
| P03-M4d (2), existing directory | Apply says "will" reuse the directory. Owner declined an extra prompt, flag or refusal. | Explains the consequence without adding a blocking policy. Ordinary files remain subject to rewriting and leak scanning; exact control artifacts and containment guards retain their existing behavior. |
| P03-M4d (3), inventory cost | `build_plan()` passes its validated snapshot to `stray_press_dirs()` and stores the advice on `Plan`; the CLI renders it. | Removes an extra capture. Each new plan still captures fresh state, and repeated reads within a capture remain stability checks. No persistent cache was added. |
| P07-T07, mapping annotation | `_parse_platforms` now takes `Mapping[str, object]`. | Makes the read-only parser's key and value types explicit without changing runtime validation. |
| P07-T07, macOS classifier | The false finding received an official-source reply and its thread was resolved. | Keeps the already-valid package classifier and closes stale review tracking. |

The five new regression cases failed before implementation and passed after
it. They cover preview/apply notices, retained and rewritten ordinary files,
nested-directory warnings, marker classification, one capture per plan, and
fresh advice from a later plan. Related inventory, engine and platform-rule
coverage, locked Ruff and source type checking also passed.

The complete local `just check` passed with `PYTEST_ADDOPTS='-n 8'`:
2,198 passed and 24 skipped in 2,754.80 seconds, followed by passing lint,
type checking, YAML, spelling and EditorConfig. Existing YAML line-length
warnings were nonblocking. The live acceptance matrix and PR gates remain
separate delivery requirements. The committed-source `just matrix` then
passed all four live cases, with six non-live cases deselected, in 63.43
seconds. Its initial R3 failure was isolated to sandbox denial of uv cache
writes. Granting access to the uv/Bun caches made R3 pass alone before the
full matrix was rerun successfully. This required no source correction.

A local seven-file fixture with ordinary `press/notes.md` was measured with
Python 3.13.14 over five complete discovery dry runs after one warm-up.
Median time decreased from 1.543497 seconds to 1.030054 seconds. Inventory Git
calls fell from 93 to 62, including `ls-files` calls from 9 to 6. This is a
fixture result on this Mac, not a general performance or Windows claim.

P07's `P07-T06` validation task is complete. PR #79 merged at
`bd520857daa967ad36780030a101418011e294c8` after a final local `just check`
reported 975 passed and 4 deselected, and `just matrix` reported 4 passed and
5 deselected. The D1–D8/P04–P06 adversarial audit found missing Bun provisioning
in general CI; commit `be7e0a7` fixed it after a failing test, then the gates
passed again. [Run 31990554477](https://github.com/smorinlabs/template-press/actions/runs/31990554477)
passed six historical Linux/macOS/Windows Python 3.12/3.13 jobs.
[Run 31990554494](https://github.com/smorinlabs/template-press/actions/runs/31990554494)
passed native POSIX and Windows acceptance at the same final head.

The mapping annotation comment
[3793637868](https://github.com/smorinlabs/template-press/pull/79#discussion_r3793637868)
received [evidence reply 3985249221](https://github.com/smorinlabs/template-press/pull/79#discussion_r3985249221)
linking the pushed fix `ee30a76` and passing checks. Its GraphQL thread,
`PRRT_kwDOS6BR086ZrvkY`, is resolved. The classifier
comment [3793638692](https://github.com/smorinlabs/template-press/pull/79#discussion_r3793638692)
is refuted by the [official PyPI list](https://pypi.org/classifiers/), which
includes `Operating System :: MacOS`. [Reply 3984555125](https://github.com/smorinlabs/template-press/pull/79#discussion_r3984555125)
records that evidence, and GraphQL confirmed thread `PRRT_kwDOS6BR086Zrvtr`
resolved. A final paginated GraphQL check confirmed both threads resolved
and no unresolved threads on PR #79. Do not duplicate these replies or
change the classifier.

The remaining delivery gate is PR #131's review and CI at its final head.
The current approval covers implementation, commits, pushes and review
disposition. Merge requires the owner's instruction. P03-M4d and P07 are
marked complete in the proposed records, effective when this PR merges.

After delivery, present further-work choices before starting a new feature.
P03-M6 is still unimplemented. The six P12 declines remain closed.

## Constraints to retain

- Optional improvements must justify their complexity with a real need.
  Preserve the six explicit declines unless their reopening criteria change.
- For a Windows failure, reproduce the failed test or job in isolation,
  validate the correction there, then retry the full batch. Do not split
  Windows jobs or count a filtered diagnostic as full-suite validation.
- Required setup and checks come from `AGENTS.md`: `just setup`, then
  `just check`; changes under `src/template_press/rebrand/` also require
  `just matrix`. Use locked tools through `uv run`.
- Preserve unrelated files and worktrees. The current task does not authorize
  their cleanup, a release, or downstream work.

## First action

In a clean disposable checkout of
`https://github.com/smorinlabs/template-press.git`, from its repository root,
inspect the delivery PR first:

```bash
gh pr view 131 --repo smorinlabs/template-press \
  --json state,mergedAt,headRefOid,statusCheckRollup,url
```

If the PR is open, finish any outstanding review or CI gate, then report it
ready for the owner's merge instruction. If merged, fetch current `main`,
confirm it contains the delivery, and present further-work choices. P03-M6
is available for scoping; its implementation has not been selected. Do not
switch the older dirty checkout for this task.

## Provenance

Refreshed on 2026-09-10 from the active session, committed repository records,
live Git state and GitHub merge/check evidence. The original handoff used a
partial transcript digest. The refreshed document carries the current owner
decisions inline and does not rely on another session's scrollback.
